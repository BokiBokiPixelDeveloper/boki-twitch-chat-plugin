# Classic Default theme placeholder

Planned conventional vertical Twitch chat theme. It will eventually contain `index.html`, `style.css`, `chat.js`, and `theme.json` after the web bridge contract is defined.
